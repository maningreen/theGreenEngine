# CC Helper

Takes a directory, prints out a list of the contents of all the files on the surface level of the directory.

## Example

```
$ ccHelper directory
[
    *directory.file contents*
    *directory.file contents*
    ...
]
```
